# WSLA Document Parsing API — Dependencies
#
# These are the additional dependencies on top of the docling package.
# Docling itself (with OCR + PDF support) must be installed separately:
#   pip install "docling[standard]"

# Core API dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic-settings>=2.3.0
pandas>=2.1.4
tabulate>=0.9.0
python-dotenv>=1.0.0

# PaddleOCR fallback engine (see src/wsla/pipeline/recover.py).
# The PaddlePaddle framework is not a declared dependency of paddleocr and is
# installed separately (the PyPI wheel is the CPU build) — see the Dockerfile:
#   pip install paddlepaddle==3.2.0
paddleocr==3.7.0

# PyTorch is not listed here: the Dockerfile installs the CUDA build
# (torch 2.14.0 from the cu126 index) before Docling, so Docling and EasyOCR
# run on the GPU. For a CPU-only local setup, install torch from
# https://download.pytorch.org/whl/cpu instead.
