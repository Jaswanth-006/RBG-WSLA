# WSLA Document Parsing API — Dependencies
#
# These are the additional dependencies on top of the docling package.
# Docling itself (with OCR + PDF support) must be installed separately:
#   pip install "docling[standard]"

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic-settings>=2.3.0
pandas>=2.1.4
tabulate>=0.9.0
onnxruntime>=1.7.0
